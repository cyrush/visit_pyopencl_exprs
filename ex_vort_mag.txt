du = grad(dims,x,y,z,vx)
dv = grad(dims,x,y,z,vy)
dw = grad(dims,x,y,z,vz)
vort_x = dw[1] - dv[2]
vort_y = du[2] - dw[0]
vort_z = dv[0] - du[1]
vort_mag = sqrt(vort_x*vort_x + vort_y*vort_y + vort_z*vort_y)